{
  "area": "language",
  "task": "lang-discover-selected-trait-method",
  "cases": [
    {
      "name": "correct-selected-public-extend",
      "expected": "pass",
      "files": {
        "extension.mbt": "pub extend Robot with Greeter::{greet}\n"
      },
      "final_text": "Exposed only greet as a public dot-call method and preserved the qualified wrapper."
    },
    {
      "name": "standalone-method-duplicates-trait-body",
      "expected": "fail",
      "files": {
        "extension.mbt": "// pub extend Robot with Greeter::{greet}\n\npub fn Robot::greet(self : Robot) -> String {\n  \"hello \\{self.0}\"\n}\n"
      },
      "final_text": "Added the requested dot-call method.",
      "must_fail_check_types": [
        "any_file_contains",
        "no_file_contains"
      ]
    },
    {
      "name": "correct-extend-but-removes-qualified-api",
      "expected": "fail",
      "files": {
        "lib.mbt": "pub trait Greeter {\n  greet(Self) -> String\n}\n\npub(all) struct Robot(String)\n\nimpl Greeter for Robot with greet(self) {\n  \"hello \\{self.0}\"\n}\n\npub extend Robot with Greeter::{greet}\n"
      },
      "final_text": "Added the selected public extend declaration.",
      "must_fail_check_types": [
        "moon"
      ]
    }
  ]
}
