---
name: Skill error report
about: The real MoonBit toolchain contradicted something a skill states
title: "[skill-error] <skill-name>: <claim topic> contradicted at <toolchain version>"
labels: bug
---

<!-- Privacy: include ONLY a fresh, generic, self-contained repro. No project code, paths, repository/package/company/product names, usernames, hostnames, setup URLs, environment variables, tokens, or environment details beyond sanitized tool versions and OS/arch. -->

**Skill and version** (frontmatter `name` + `metadata.skill-version`):

**Claim** (reference file + quoted sentence/example):

**Expected (per the skill) vs observed** (preserve diagnostic meaning and error codes, but sanitize paths, names, URLs, and values):

**Minimal repro** (standalone; generic names only):

**`moon version --all` output and OS/arch**:
