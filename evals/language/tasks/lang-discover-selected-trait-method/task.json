{
  "id": "lang-discover-selected-trait-method",
  "risk_class": "silent-api-surface",
  "claim_id": "language.traits.selected-public-extend-discovery",
  "primary_metric": "downstream-selected-dot-call",
  "prompt": "`Robot` already implements `Greeter`, and the library currently exposes a qualified wrapper. Change the public API so callers in another package can also write `robot.greet()`. Only `greet` should become a dot-call method; do not duplicate its body as a standalone `Robot` method or change the trait implementation. I do not know MoonBit's name for this feature—find the current syntax. Keep the existing qualified API and make checks/tests pass with warnings denied.",
  "grader_note": "2026-07-18: discoverability regression whose clean starter avoids compiler diagnostics that reveal the declaration name; hidden cross-package tests require both the selected dot-call method and the existing qualified wrapper.",
  "discovery": {
    "skill": "moonbit-language",
    "reference": "references/traits-and-generics.mbt.md"
  },
  "grade": [
    {
      "type": "any_file_contains",
      "glob": "*.mbt",
      "regex": "(?:^|\\n)[ \\t]*pub[ \\t]+extend[ \\t]+Robot[ \\t]+with[ \\t]+Greeter::\\{[ \\t]*greet[ \\t]*,?[ \\t]*\\}[ \\t]*(?://[^\\n]*)?(?:\\n|$)"
    },
    {
      "type": "no_file_contains",
      "glob": "*.mbt",
      "regex": "(?:^|\\n)\\s*(?:pub\\s+)?fn\\s+Robot::greet\\s*\\("
    },
    {
      "type": "file_contains",
      "path": "lib.mbt",
      "regex": "impl\\s+Greeter\\s+for\\s+Robot\\s+with\\s+greet\\s*\\(self\\)\\s*\\{\\s*\"hello\\s+\\\\\\{self\\.0\\}\"\\s*\\}"
    },
    {
      "type": "moon",
      "args": [
        "check",
        "--deny-warn"
      ]
    },
    {
      "type": "moon",
      "args": [
        "test",
        "--deny-warn"
      ],
      "min_tests": 3,
      "temp_files": {
        "consumer/zz_eval_dot_test.mbt": "test \"grader public selected method\" {\n  assert_eq(@api.Robot(\"C3\").greet(), \"hello C3\")\n}\n\ntest \"grader qualified API remains\" {\n  assert_eq(@api.greet_qualified(@api.Robot(\"Q7\")), \"hello Q7\")\n}\n"
      }
    }
  ]
}
