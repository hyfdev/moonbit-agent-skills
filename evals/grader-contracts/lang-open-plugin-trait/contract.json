{
  "area": "language",
  "task": "lang-open-plugin-trait",
  "cases": [
    {
      "name": "opens-only-the-plugin-trait",
      "expected": "pass",
      "files": {
        "lib.mbt": "///|\npub(open) trait Codec {\n  encode(Self) -> String\n}\n\n///|\npub trait BuiltinCodec {\n  builtin_name(Self) -> String\n}\n\n///|\npub(all) struct JsonCodec {}\n\n///|\nimpl Codec for JsonCodec with encode(_self) {\n  \"json\"\n}\n\n///|\nimpl BuiltinCodec for JsonCodec with builtin_name(_self) {\n  \"json\"\n}\n\n///|\npub fn encode_json() -> String {\n  Codec::encode(JsonCodec::{})\n}\n\n///|\npub fn builtin_json_name() -> String {\n  BuiltinCodec::builtin_name(JsonCodec::{})\n}\n"
      }
    },
    {
      "name": "leaves-the-plugin-trait-sealed",
      "expected": "fail",
      "files": {
        "lib.mbt": "///|\npub trait Codec {\n  encode(Self) -> String\n}\n\n///|\npub trait BuiltinCodec {\n  builtin_name(Self) -> String\n}\n\n///|\npub(all) struct JsonCodec {}\n\n///|\nimpl Codec for JsonCodec with encode(_self) {\n  \"json\"\n}\n\n///|\nimpl BuiltinCodec for JsonCodec with builtin_name(_self) {\n  \"json\"\n}\n\n///|\npub fn encode_json() -> String {\n  Codec::encode(JsonCodec::{})\n}\n\n///|\npub fn builtin_json_name() -> String {\n  BuiltinCodec::builtin_name(JsonCodec::{})\n}\n"
      },
      "must_fail_check_types": ["file_contains", "moon"]
    },
    {
      "name": "opens-both-public-traits",
      "expected": "fail",
      "files": {
        "lib.mbt": "///|\npub(open) trait Codec {\n  encode(Self) -> String\n}\n\n///|\npub(open) trait BuiltinCodec {\n  builtin_name(Self) -> String\n}\n\n///|\npub(all) struct JsonCodec {}\n\n///|\nimpl Codec for JsonCodec with encode(_self) {\n  \"json\"\n}\n\n///|\nimpl BuiltinCodec for JsonCodec with builtin_name(_self) {\n  \"json\"\n}\n\n///|\npub fn encode_json() -> String {\n  Codec::encode(JsonCodec::{})\n}\n\n///|\npub fn builtin_json_name() -> String {\n  BuiltinCodec::builtin_name(JsonCodec::{})\n}\n"
      },
      "must_fail_check_types": ["file_not_contains", "moon"]
    },
    {
      "name": "hides-the-sealed-trait-and-hard-codes-the-wrapper",
      "expected": "fail",
      "files": {
        "lib.mbt": "///|\npub(open) trait Codec {\n  encode(Self) -> String\n}\n\n///|\ntrait BuiltinCodec {\n  builtin_name(Self) -> String\n}\n\n///|\npub(all) struct JsonCodec {}\n\n///|\nimpl Codec for JsonCodec with encode(_self) {\n  \"json\"\n}\n\n///|\nimpl BuiltinCodec for JsonCodec with builtin_name(_self) {\n  \"json\"\n}\n\n///|\npub fn encode_json() -> String {\n  Codec::encode(JsonCodec::{})\n}\n\n///|\npub fn builtin_json_name() -> String {\n  \"json\"\n}\n"
      },
      "must_fail_check_types": ["file_contains", "moon"]
    }
  ]
}
