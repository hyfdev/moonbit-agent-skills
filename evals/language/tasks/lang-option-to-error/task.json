{
  "id": "lang-option-to-error",
  "prompt": "Refactor parse_port to use MoonBit's checked error handling (a dedicated error type that the function raises on invalid input) instead of returning an Option. Update the tests accordingly; moon test must pass.",
  "grader_note": "2026-07-17: require the package, implementation, and updated tests to remain and run nonzero hidden valid/invalid behavior checks; deleting files, leaving an unrecognized package, or weakening the rewritten tests previously could pass.",
  "grade": [
    {"type": "file_exists", "path": "moon.mod"},
    {"type": "file_exists", "path": "moon.pkg"},
    {"type": "file_exists", "path": "lib.mbt"},
    {"type": "file_exists", "path": "lib_test.mbt"},
    {"type": "file_contains", "path": "lib.mbt", "regex": "pub fn\\s+parse_port"},
    {"type": "file_contains", "path": "lib_test.mbt", "regex": "parse_port"},
    {"type": "moon", "args": ["check"]},
    {"type": "moon", "args": ["test"]},
    {"type": "any_file_contains", "glob": "*.mbt", "regex": "suberror"},
    {"type": "any_file_contains", "glob": "*.mbt", "regex": "raise"},
    {
      "type": "moon",
      "args": ["test"],
      "min_tests": 1,
      "temp_files": {
        "zz_eval_parse_port_test.mbt": "///|\ntest \"grader preserves parse behavior\" {\n  let valid = try parse_port(\"8080\") catch { _ => -1 }\n  assert_eq(valid, 8080)\n  try parse_port(\"80x0\") catch {\n    _ => ()\n  } noraise {\n    _ => fail(\"expected invalid input to raise\")\n  }\n}\n"
      }
    }
  ]
}
