{
  "id": "lang-immutable-constructor-migration",
  "risk_class": "warning-only-migration",
  "claim_id": "language.immutable.type-named-constructor",
  "primary_metric": "warning-free-behavior-preservation",
  "prompt": "Update this MoonBit 0.10.4 package to stop using the deprecated immutable-container constructor. Migrate it to the current type-named `HashSet` constructor while preserving behavior. Run the compiler with warnings denied and keep the test passing.",
  "grader_note": "Release-derived regression: reject from_array and require the type-named HashSet constructor introduced in 0.10.4.",
  "grade": [
    {
      "type": "file_contains",
      "path": "lib.mbt",
      "regex": "@hashset\\.HashSet\\s*\\("
    },
    {
      "type": "moon",
      "args": [
        "check",
        "--deny-warn"
      ]
    },
    {
      "type": "moon",
      "args": [
        "test",
        "--deny-warn"
      ],
      "min_tests": 1,
      "temp_files": {
        "zz_eval_unique_count_test.mbt": "test \"grader constructor preserves general behavior\" {\n  assert_eq(unique_count([]), 0)\n  assert_eq(unique_count([5, 5, 5]), 1)\n  assert_eq(unique_count([1, 2, 3, 4]), 4)\n  assert_eq(unique_count([3, 1, 3, 2, 1, 4]), 4)\n}\n"
      }
    }
  ]
}
