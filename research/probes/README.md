# Raw probe evidence (session artifacts, preserved verbatim)

These are the unedited findings files from the five probe batteries that ran against the pinned toolchain (moon 0.1.20260713 / moonc v0.10.4) on 2026-07-17, plus the probe protocol they followed (COMMON.md). They are the provenance trail for the skills' content — see research/methodology.md Phase 2.

Only mechanical change from the originals: the ephemeral session-workspace path prefix is replaced with `<session-workspace>`. Scratch probe modules themselves were not preserved; every finding records its exact code and command, so any entry can be re-run from the file alone.
