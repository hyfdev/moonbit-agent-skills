interface:
  display_name: "MoonBit Language"
  short_description: "Verified MoonBit syntax, semantics, migrations, and diagnostics"
  default_prompt: "Use $moonbit-language when understanding, writing, reviewing, or fixing MoonBit code (.mbt / .mbt.md), including current function and argument syntax, structs and enums, patterns, traits and explicit extend/pub extend, visibility, checked errors, async, tests, and FFI declarations."

policy:
  allow_implicit_invocation: true
